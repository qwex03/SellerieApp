<form method="post" action="{{ path('app_emplacement_delete', {'id': emplacement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emplacement.id) }}">
    <button class="px-4 py-2 bg-red-500 text-white rounded-md shadow hover:bg-red-600 focus:ring-2 focus:ring-red-400 focus:outline-none">Supprimer</button>
</form>
